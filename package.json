{
  "name": "eyechat-client",
  "version": "0.1.0",
  "description": "EyeChat React front-end. Normally built from the parent eyechat-server checkout (`npm run build` there); this manifest exists so the client can also be installed and run on its own against a remote or local API.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "prop-types": "^15.8.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "recharts": "^3.9.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "vite": "^5.4.21"
  }
}
